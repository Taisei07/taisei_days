<%= form_with(model: nappi, local: true) do |form| %>
  <% if nappi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nappi.errors.count, "error") %> prohibited this nappi from being saved:</h2>

      <ul>
      <% nappi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :year %>
    <%= form.text_field :year %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
